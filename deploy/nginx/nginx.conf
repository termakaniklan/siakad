# Contoh konfigurasi Nginx untuk SIAKAD (baremetal).
#
# Cara pakai:
#   - Salin file ini ke `/etc/nginx/nginx.conf` (REPLACE total) bila Anda
#     mengelola Nginx dari nol; ATAU
#   - Cukup ambil bagian `limit_req_zone` & `gzip_types` ke dalam snippet
#     terpisah di `/etc/nginx/conf.d/` bila Anda mempertahankan default distro.
# Site-spesifik (proxy_pass dst.) ada di `deploy/nginx/conf.d/default.conf`.
#
# `user`: bawaan Nginx Debian/Ubuntu = `www-data`. Sesuaikan bila distro Anda
# memakai user berbeda (mis. `nginx` pada RHEL/Rocky/AlmaLinux).
user  www-data;
worker_processes auto;
worker_rlimit_nofile 16384;

events {
    worker_connections 4096;
    multi_accept on;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    server_tokens off;
    sendfile      on;
    tcp_nopush    on;
    tcp_nodelay   on;
    keepalive_timeout 65;
    types_hash_max_size 4096;
    client_max_body_size 25m;

    # Logs
    log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
                      '$status $body_bytes_sent "$http_referer" '
                      '"$http_user_agent" "$http_x_forwarded_for"';
    access_log /var/log/nginx/access.log main;
    error_log  /var/log/nginx/error.log warn;

    # Compression
    gzip on;
    gzip_comp_level 5;
    gzip_min_length 1024;
    gzip_proxied any;
    gzip_vary on;
    gzip_types
        application/atom+xml
        application/javascript
        application/json
        application/manifest+json
        application/rss+xml
        application/vnd.ms-fontobject
        application/x-font-ttf
        application/x-web-app-manifest+json
        application/xhtml+xml
        application/xml
        font/opentype
        image/svg+xml
        image/x-icon
        text/css
        text/plain
        text/xml;

    # Rate limiting zones — applied per route in conf.d/default.conf
    limit_req_zone $binary_remote_addr zone=login:10m rate=5r/m;
    limit_req_zone $binary_remote_addr zone=api:10m   rate=120r/m;
    limit_req_zone $binary_remote_addr zone=ppdb:10m  rate=8r/m;

    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header Connection "";
    proxy_read_timeout 60s;

    include /etc/nginx/conf.d/*.conf;
}
